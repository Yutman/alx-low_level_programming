#include <stdio.h>

/**
  * main - Print all single digit numbers
  * Return: Return 0 when program is successful
  */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%i", i);
	putchar('\n');

	return (0);

}
