#include <stdio.h>

/**
  * main - Program start
  * Return: Returns 0 when program success
  */
int main(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);

}
