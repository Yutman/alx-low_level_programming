#include "main.h"

/**
  *_abs - gives absolute value of integer
  * @p: input number as an integer
  * Return: Returns the absolute value of an integer
  */
int _abs(int p)
{
	if (p >= 0)
	{
		return (p);
	}
	else
	{
		return (n * -1);
	}
}
